imports:
    - { resource: config_dev.yml }

framework:
    test: ~
    session:
        storage_id: session.storage.mock_file
    profiler:
        collect: false

web_profiler:
    toolbar: false
    intercept_redirects: false

app:
    master:
        storage: local
        path:    "vfs://root/source"
        filters:
            path: { pattern: "~[A-Z]{4}/stream/.*\.mov~" }
            exclude: { path: "vfs://root/exclude.json" }
    slave:
        storage: local
        path:    "vfs://root/dest"
        path_tpl: "/{program}/{uid}"
        filters: